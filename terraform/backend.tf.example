# Copy this file to backend.tf and replace the placeholders before terraform init.
terraform {
  backend "s3" {
    bucket         = $BUCKET_NAME
    key            = "eks-karpenter/terraform.tfstate"
    region         = "eu-central-1"
    encrypt        = true
    use_lockfile   = true
  }
}
